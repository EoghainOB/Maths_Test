import React from "react";

const Selection = () => {
  return (
    <form>
      <button>Easy</button>
      <button>Medium</button>
      <button>Hard</button>
    </form>
  );
};

export default Selection;
